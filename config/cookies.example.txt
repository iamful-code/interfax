# Скопируйте этот файл в config/cookies.txt и подставьте значения из браузера (F12 -> Network ->
# первый запрос -> Request Headers). Файл содержит учётные данные сессии: в git он не попадает.
#
# 1) строка User-Agent из того же браузера (защита связывает cookies с User-Agent):
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/152.0.0.0 Safari/537.36
#
# 2) значение заголовка Cookie одной строкой (всё после "cookie:"):
spjs=...; spsc=...; spid=...; .AspNetCore.Antiforgery.xxxx=...
